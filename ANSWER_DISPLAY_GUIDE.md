# 📱 Инструкция: Просмотр ответов с телефона

## Как это работает

Когда вы открываете вопрос в игре "Морской бой", он автоматически сохраняется в localStorage браузера. Специальная страница `answer-display.html` читает эти данные и отображает текущий вопрос и ответ в реальном времени.

## Настройка

### Шаг 1: Раздать WiFi с компьютера (Linux)

**Вариант A: Через GUI (Network Manager)**
1. Откройте настройки сети
2. Выберите "Create Wi-Fi Hotspot" / "Создать точку доступа"
3. Задайте имя сети и пароль
4. Включите hotspot

**Вариант B: Через командную строку**

```bash
# Создать hotspot с именем "BattleshipGame" и паролем "quiz2024"
nmcli device wifi hotspot ssid BattleshipGame password quiz2024

# Проверить IP адрес вашего компьютера
ip addr show | grep "inet "
# Ищите IP вида 10.42.0.1 или 192.168.x.x
```

### Шаг 2: Запустить сервер разработки с доступом по сети

```bash
cd /home/sysadmin/TESTS/battleship

# Запустить Vite с доступом из сети
npm run dev -- --host
```

Вы увидите что-то вроде:
```
  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.1.100:5173/
```

Запомните Network адрес!

### Шаг 3: Подключиться с телефона

1. **На телефоне**: подключитесь к WiFi сети "BattleshipGame" (пароль: quiz2024)

2. **Откройте два окна в браузере телефона:**
   
   **Окно 1 - Основная игра:**
   ```
   http://192.168.1.100:5173/
   ```
   (замените на ваш Network адрес из Шага 2)
   
   **Окно 2 - Просмотр ответов:**
   ```
   http://192.168.1.100:5173/answer-display.html
   ```

3. **На компьютере**: откройте игру в браузере
   ```
   http://localhost:5173/
   ```

## Использование

### Основной сценарий:

1. **Компьютер (ведущий)**: Играет в обычную игру
   - Начинает игру, выбирает режим
   - Нажимает на клетки, открывает вопросы
   
2. **Телефон**: Показывает ответы
   - Страница автоматически обновляется каждые 500 мс
   - Как только на компьютере откроется вопрос, на телефоне сразу появится вопрос И ответ
   - Когда вопрос закроется - дисплей очистится

### Что показывается на телефоне:

- ✅ Статус игры (активна/не активна)
- ❓ Текст вопроса
- ✅ **ОТВЕТ на вопрос**
- 📂 Категория
- ⭐ Сложность
- 🎯 Количество баллов
- 📎 Информация о медиафайле (если есть)

## Альтернативные варианты

### Вариант 1: Использовать туннель (если нет WiFi адаптера)

```bash
# Установить localtunnel
npm install -g localtunnel

# Запустить игру
npm run dev

# В другом терминале создать туннель
lt --port 5173
```

Вы получите публичный URL типа `https://random-name.loca.lt`, который можно открыть с телефона.

### Вариант 2: USB-tethering

1. Подключите телефон к компьютеру по USB
2. Включите USB-модем на телефоне
3. На компьютере появится новый сетевой интерфейс
4. Запустите `npm run dev -- --host`
5. На телефоне откройте браузер и подключитесь к IP компьютера

## Полезные команды

```bash
# Проверить запущен ли hotspot
nmcli device status

# Остановить hotspot
nmcli connection down Hotspot

# Посмотреть какие устройства подключены
arp -a

# Проверить firewall (если телефон не подключается)
sudo ufw status
sudo ufw allow 5173/tcp
```

## Решение проблем

**Телефон не видит сеть:**
- Убедитесь что WiFi адаптер компьютера поддерживает режим AP (Access Point)
- Попробуйте создать hotspot через GUI

**Не открывается страница:**
- Проверьте firewall: `sudo ufw allow 5173/tcp`
- Убедитесь что используете правильный IP адрес
- Проверьте что Vite запущен с флагом `--host`

**Ответы не отображаются:**
- Убедитесь что основная игра запущена на компьютере
- Откройте вопрос в основной игре
- Проверьте консоль браузера на телефоне (F12 в мобильных браузерах)
- localStorage может не работать в приватном режиме - используйте обычный режим

**Страница не обновляется:**
- Обновите страницу вручную
- Проверьте что обе вкладки (компьютер и телефон) используют один и тот же домен (оба localhost или оба Network IP)

## Безопасность

⚠️ **Важно:** 
- Не используйте слабый пароль для WiFi hotspot на публичных мероприятиях
- Vite dev server не предназначен для production использования
- Для публичных игр рассмотрите использование VPN или более защищенного решения

## Дополнительные возможности

Можно открыть `answer-display.html` на нескольких устройствах одновременно:
- Телефон ведущего (чтобы видеть ответ)
- Планшет помощника
- Второй монитор/ноутбук

Все они будут показывать одинаковую информацию синхронно!
