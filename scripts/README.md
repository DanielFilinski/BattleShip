# Скрипт генерации позиций кораблей и бомб

## Описание

Этот скрипт автоматически генерирует новые позиции для кораблей и бомб на игровом поле, сохраняя привязку вопросов к каждому кораблю и бомбе.

## Особенности

- **Сохранение вопросов**: Все вопросы остаются привязанными к тем же кораблям и бомбам
- **Правило расстояния**: Каждый объект (корабль или бомба) находится на расстоянии минимум 1 клетки от других объектов (включая диагонали)
- **Случайная генерация**: Позиции генерируются случайным образом с соблюдением всех правил
- **Визуализация**: Создается наглядная визуализация поля в консоли и в файле `Positions.md`

## Использование

### Запуск скрипта

```bash
npm run generate-positions
```

### Что делает скрипт

1. Читает текущие данные из файлов:
   - `public/data/ships.json` - корабли с вопросами
   - `public/data/bombs.json` - бомбы с вопросами
   - `public/data/questions.json` - все вопросы

2. Генерирует новые позиции для всех кораблей:
   - 1 линкор (4 клетки)
   - 2 крейсера (3 клетки каждый)
   - 3 эсминца (2 клетки каждый)
   - 4 подлодки (1 клетка каждая)

3. Генерирует новые позиции для бомб (5 штук)

4. Сохраняет обновленные данные обратно в файлы `ships.json` и `bombs.json`

5. Создает визуализацию:
   - Выводит в консоль красивую таблицу с расположением кораблей и бомб
   - Сохраняет детальную информацию в файл `Positions.md`

### Выходные файлы

После выполнения скрипта создаются/обновляются следующие файлы:

- **`public/data/ships.json`** - обновленные позиции кораблей
- **`public/data/bombs.json`** - обновленные позиции бомб
- **`Positions.md`** - детальная визуализация и информация о позициях

## Формат данных

### Корабли (ships.json)

```json
{
  "ships": [
    {
      "id": "ship1",
      "name": "Линкор",
      "cells": ["А1", "Б1", "В1", "Г1"],
      "questionIds": ["q1", "q2", "q3", "q4"]
    }
  ]
}
```

### Бомбы (bombs.json)

```json
{
  "bombs": [
    {
      "cell": "Д5",
      "questionId": "q1"
    }
  ]
}
```

## Правила размещения

1. Корабли могут располагаться горизонтально или вертикально
2. Между кораблями и бомбами должна быть минимум 1 пустая клетка
3. Проверяются все 8 соседних клеток (включая диагонали)
4. Все объекты должны находиться в пределах игрового поля (10x10)

## Визуализация

В файле `Positions.md` вы найдете:

- Таблицу с визуализацией поля
- Легенду (номера кораблей и символы)
- Подробную информацию о каждом корабле:
  - Позиция на поле
  - Привязанные вопросы для каждой клетки
- Информацию о бомбах
- Статистику (количество объектов, процент заполнения поля)

## Пример вывода

```
┌───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┐
│   │ А │ Б │ В │ Г │ Д │ Е │ Ж │ З │ И │ К │
├───┼───┼───┼───┼───┼───┼───┼───┼───┼───┼───┤
│  1│ 💣 │ · │ 2 │ · │ 6 │ · │ · │ · │ 💣 │ · │
├───┼───┼───┼───┼───┼───┼───┼───┼───┼───┼───┤
│  2│ · │ · │ 2 │ · │ · │ · │ 8 │ · │ · │ · │
└───┴───┴───┴───┴───┴───┴───┴───┴───┴───┴───┘
```

Где:
- `·` - пустая клетка
- `1-10` - номера кораблей
- `💣` - бомба

## Устранение неполадок

Если скрипт не может разместить корабли или бомбы (слишком много объектов для размера поля), он выведет сообщение об ошибке и завершится с кодом 1.

В этом случае проверьте:
- Размер игрового поля (по умолчанию 10x10)
- Количество и размер кораблей
- Количество бомб

## Технические детали

- Язык: TypeScript
- Максимальное количество попыток размещения объекта: 100
- Поддержка ES модулей
- Использует криптографически безопасную случайность для генерации позиций
